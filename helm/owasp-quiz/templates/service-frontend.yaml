
apiVersion: v1
kind: Service
metadata:
  name: quiz-frontend
  namespace: {{ .Release.Namespace }}
  annotations:
    # Indicate backend uses HTTPS
    nginx.ingress.kubernetes.io/backend-protocol: "HTTPS"
    caddy.ingress.kubernetes.io/backend-protocol: "https"
    caddy.ingress.kubernetes.io/upstreams: "https://quiz-frontend:443"
    caddy.ingress.kubernetes.io/tls_insecure_skip_verify: "true"
spec:
  selector: { app: quiz-frontend }
  ports:
  - port: 443
    targetPort: 443
    appProtocol: https
    name: https
